# 酒馆助手 Lite · 功能说明与制卡指南

版本 **0.2.1** ｜ 仓库 <https://github.com/maoyao253/tavern-helper-lite>

---

## 一、这是什么

给**老内核酒馆**用的轻量扩展：**HTML 前端渲染 + MVU 变量 + 卡内脚本宿主**。
零静态 `import`，全部能力通过 `window.SillyTavern.getContext()` 获取，可在 SillyTavern **1.12.x ~ 1.18.x** 上加载。

**为什么需要它**：官方酒馆助手 3.3+ / 4.x 会在加载时 `import` 新内核才有的导出
（`getCurrentLocale`、`showFontAwesomePicker`、`extensionTypes`、`stopGeneration`、
`world_info_include_names` …）。ES 模块导入不存在的具名导出是**链接期 SyntaxError**，
整个扩展不执行 —— 所以它在 1.12.3 这类老内核上"装了但什么都不显示"。本扩展不 import 任何 ST 模块，
因此结构上不会出现这个问题。

---

## 二、功能清单

| 功能 | 说明 | 默认 |
|---|---|---|
| 前端渲染 | 消息里的 ` ```html ` 代码块（或自动识别像 HTML 的块）渲染进 `sandbox="allow-scripts"` 的 iframe，按内容自动调高 | 开 |
| 自动识别 HTML | 未标注语言的块，若以 `<` 开头且含闭合标签也渲染 | 开 |
| 隐藏控制块 | `<UpdateVariable>` 从正文隐藏；兼容 ST 清洗掉标签、空白折叠、块被拆成多节点三种形态 | 开 |
| MVU 变量回放 | 回放整段对话的 `<UpdateVariable>`，支持 JSON Patch 数组 / JSON 对象 / `key: value` 行 | 开 |
| 初始变量 | 从角色卡的 `tavern_helper.variables` 播种再叠加消息更新 | 开 |
| 变量落库 | 结果写入 ST 原生 `chat_metadata.variables`，因此 `{{getvar::x}}` 在提示词、STscript、前端里都可用 | 开 |
| 变量表注入 | 把变量树作为提示词片段注入（位置 IN_CHAT，深度可调） | 开 · 深度 2 |
| AI 自动变化 | AI 回复触发变量回放 → 界面卡片**自动重画**，无需点按钮；划卡/编辑/删除楼层同样自动重放 | 开 |
| 卡内脚本宿主 | 读取当前角色卡的 `tavern_helper.scripts` 并按序执行（支持 `import`/`export`/顶层 await） | 开 |
| MVU 兼容层 | 提供 `Mvu` / `eventOn` / `eventOnce` / `eventOff` / `eventEmit` / `waitGlobalInitialized` | 开 |
| 面板工具 | 重绘全部楼层、重算变量、自检、诊断正文、渲染预览、变量表查看、注入深度 | — |

---

## 三、实现原理

### 3.1 零 import + 快照语义

* 不 `import` 任何 ST 模块 → 不存在链接期失败。
* **`getContext()` 返回的是快照对象**：`characterId` / `chat` / `chatMetadata` 都是调用那一刻的值。
  启动时取一次会永远停在"还没选角色"的状态，所以动态字段每次重新取（内部 `liveCtx()`）。
  这一点是 v0.2.0 前的实际 bug，卡内脚本因此一直读不到。

### 3.2 渲染管线（单条防抖管线，120 ms）

```
事件（消息渲染/划卡/编辑/收到回复/切换对话）
   └─► 变量回放（recomputeIfChanged：变了才落盘+重推提示词）
        └─► renderAll()：代码块 → 沙箱 iframe
             └─► refreshFrames()：含 {{...}} 的已渲染块按新变量重画
                  └─► 面板刷新
```

* `sandbox="allow-scripts"`（**不给** `allow-same-origin`）：卡内前端拿不到酒馆页面 DOM。
* 高度：iframe 内脚本测量"所有元素下边缘的最大值"（因此绝对定位、`100vh` 的卡片也能撑开），
  经 `postMessage` 回报；`MutationObserver` + `ResizeObserver` + 图片 load + 1s 轮询触发重测。

### 3.3 变量系统

* 解析 `<UpdateVariable>…</UpdateVariable>`，依次应用 JSON Patch（`add/replace/remove/move/copy`，
  `-` = 数组追加）或 JSON 对象合并，退化为 `key: value` 行解析。
* 嵌套对象压平成 `父.子` 写进 ST 原生变量表；完整树保留给注入与 `Mvu.getMvuData()`。
* 宏替换顺序：**先填自家变量再交给 ST 的宏引擎** —— 否则老内核可能先把 `{{getvar::x}}` 替成空值。

### 3.4 卡内脚本宿主

* 数据来源：`characters[characterId].data.extensions.tavern_helper.scripts`（只跑 `enabled !== false`）。
* 执行方式：优先 Blob + 动态 `import()`（支持 `import`/`export`/顶层 await），环境不允许时回退 `new Function` 包装。
* 自动跳过自带 MVU/Zod 打包的脚本（命中 `MagVarUpdate` / `mvu_zod` / `registerMvuSchema`），
  避免和 Lite 的兼容层抢全局。
* 同一角色只跑一次（防重复挂载悬浮窗）；换角色时清掉上一批脚本往 `document.body` 新增的节点。
* 变量变化时派发 `mvu:variable_initialized` / `mvu:variable_update_ended`，卡内脚本据此刷新界面。

---

## 四、制卡指南（想用这些功能，卡要怎么写）

### 4.1 HTML 前端

在消息里用围栏代码块输出，语言标 `html`：

````
```html
<div style="padding:12px;border-radius:12px;background:#2b2b3d;color:#eee">
  <b>{{char}}</b> 对 <b>{{user}}</b> 的好感度：{{getvar::好感度}}
</div>
```
````

* 未标语言的块，只要以 `<` 开头并含 `</标签>` 也会被渲染（可在面板关掉）。
* 块内含 `<UpdateVariable>` 的不会被误判成 HTML。
* 可用宏：`{{char}}`、`{{user}}`、`{{getvar::键}}`、`{{getvar::父.子}}`（嵌套用点号）。
* iframe 是**沙箱**的：不能读酒馆页面 DOM、不能访问 `window.parent`；需要交互就往脚本宿主里放。
* 尺寸：外层容器不用写死高度，写死也行（测量按元素下边缘，不会被裁）。

### 4.2 变量更新（MVU 风格）

```xml
<!-- 直接给值（对象合并） -->
<UpdateVariable>{"好感度": 42, "关系": {"阶段": "初识"}}</UpdateVariable>

<!-- JSON Patch（推荐，可增量） -->
<UpdateVariable>[{"op":"replace","path":"/好感度","value":55},
                 {"op":"add","path":"/物品/-","value":"玉佩"}]</UpdateVariable>

<!-- 退化写法 -->
<UpdateVariable>好感度: 42</UpdateVariable>
```

* 更新块会自动从正文隐藏，变量随 AI 回复自动生效、界面自动重画。
* 写成 `关系.阶段` 这样的扁平键，就能用 `{{getvar::关系.阶段}}`。

### 4.3 初始变量

把初值放在卡片的 `data.extensions.tavern_helper.variables`：

```json
{ "好感度": 0, "关系": { "阶段": "初识" }, "物品": [] }
```

Lite 用它作为回放起点（MVU 的"初始变量"语义）。**注意**：此机制已实现，但尚未用非空数据实测过。

### 4.4 卡内脚本（悬浮窗、状态约束等）

在 `data.extensions.tavern_helper.scripts` 放脚本数组：

```json
[{
  "name": "floating-window",
  "type": "script",
  "enabled": true,
  "content": "(() => { /* … */ })();"
}]
```

脚本运行在**酒馆页面顶层**（不是沙箱 iframe），因此：

* 可以直接建 DOM、`document.body.appendChild(...)`，用 `position:fixed` 做悬浮窗；
  `window.parent === window`，所以标准的"宿主文档外壳"写法（`window.parent?.document || document`）天然可用。
* 可用全局：`Mvu`、`eventOn` / `eventOnce` / `eventOff` / `eventEmit`、`waitGlobalInitialized`、
  `SillyTavern`（含 `getContext()`）、`$`、`toastr`。
* 可以 `import('https://…')` 从 CDN 取资源，支持顶层 `await` 与 `export`。
* 读变量：`Mvu.getMvuData({type:'message', message_id:'latest'}).stat_data`；
  订阅变化：`eventOn(Mvu.events.VARIABLE_UPDATE_ENDED, fn)`。
* 悬浮窗范例：脚本首次执行时建壳（可在 `document.body` 上挂 `position:fixed` 的容器），
  之后只靠事件刷新内容；Lite 换角色时会自动清掉你新增到 `body` 的节点。
* **不要**在卡里再打包一份 MVU/Zod（会被跳过）；数值约束这类纯逻辑脚本（如 `enforce`）可以正常跑。

### 4.5 提示词侧

* 变量表会自动注入到对话（默认深度 2）。想自己控制，就在卡里写"请以 JSON 更新变量"之类的指令，
  不必依赖 `{{getvar}}` 进提示词。
* 变量写在 `chat_metadata.variables`，因此 STscript 里 `{{getvar::键}}` 同样可用。

---

## 五、已知问题与限制（截至 v0.2.1）

**功能缺口**

1. **不是官方酒馆助手**：没有脚本库 UI、没有 `TavernHelper.*` API 面（`getChatMessages`、
   `setVariables`、楼层操作等）。卡里若调用 `TavernHelper.xxx` 会报错。
2. **MVU 兼容层是只读的**：脚本若要**写**变量（`Mvu.setMvuVariable`、`replaceVariables` 等）不会生效 ——
   例如 `enforce` 虽然"已执行"，它算出的修正值目前**写不回**变量表。
3. **跳过 MVU/Zod 打包脚本**：因此 Zod schema 校验、`prefault` 默认值、MVU 自己的解析配置都不生效；
   变量解析完全由 Lite 的宽松解析器负责。
4. `getMvuData()` 忽略 `message_id`，永远返回"最新状态"；没有 `display_data`、没有楼层级变量作用域。
5. 正文里的 `{{getvar::}}` 只在**渲染后的 HTML**中被替换；发给模型的正文不做替换（靠变量表注入补）。
6. 变量回放不绑定卡片自带的"隐藏变量更新块"正则；若卡片同时装了那条正则，两者会各删一次（无害但重复）。

**体验问题**

7. 控制块被隐藏后，如果那一楼只剩它，会留下**空的消息气泡**。
8. iframe 是沙箱的：卡内前端不能读酒馆页面状态，也不能和宿主页面直接通信（只能靠变量）。
9. 扩展更新需手动：「管理扩展」→ ⬇ 更新 → **Ctrl + F5**（`auto_update: false`，避免被上游覆盖）。
10. 变量没有清理机制：删扩展后 `chat_metadata.variables` 里的值仍留在聊天文件中。

**未验证 / 环境相关**

11. 只在 **SillyTavern 1.12.3（托管实例）** 与 **1.18.0（本机）** 上验证过；其它版本未测。
12. 初始变量（4.3）机制已实现，**未用非空数据实测**。
13. 卡内脚本以**完整页面权限**运行（与官方酒馆助手一致）——**只运行你信任的卡**；
    消息 iframe 有沙箱，但脚本没有。
14. 平台自身的坑：托管实例的 `i18n.js:234` 有个初始化报错（与本扩展无关）。

---

## 六、安装与更新

**托管实例（用安装扩展面板）**

1. 新建公开仓库（GitHub / Gitee），放入 `manifest.json` 与 `index.js` 两个文件。
2. 「安装扩展」填仓库地址 → 刷新页面。
3. 更新：改仓库 → 「管理扩展」点 ⬇ → **Ctrl + F5**（面板底部显示版本号，可用来确认）。

**本地酒馆**

把 `manifest.json` 与 `index.js` 放进
`<酒馆目录>/public/scripts/extensions/third-party/TavernHelperLite/`，刷新页面。

**排错**

* 面板「自检」：列出各 API 是否可用、变量键数、卡内脚本逐个的执行结果。
* 面板「诊断正文」：逐楼层导出「原文 vs 渲染后 HTML」，用于定位正文里残留的控制块。
* 控制台 `[酒馆助手Lite]` 前缀的日志；`window.ThLite` 暴露 `recompute` / `renderAll` / `probe` / `dumpDiagnostics`。

---

## 七、验收记录

| 环境 | 方式 | 结果 |
|---|---|---|
| SillyTavern 1.12.3 真机实例 | 无头 Chrome + CDP 注入源码运行 | **25/25 通过** |
| SillyTavern 1.18.0 本机 | 真实安装 + 载入真实角色卡 | 悬浮窗挂载成功（`#ghr-hud` / `#ghr-ball` / `#ghr-hud-style`），4 个卡内脚本按策略执行/跳过 |

覆盖：扩展执行、面板挂载、API 自检、HTML→沙箱 iframe、内容与自动高度（含绝对定位/100vh）、
JSON Patch 覆盖、嵌套变量压平、数组追加、AI 自动回放、已渲染卡片自动重画、控制块隐藏（三种形态）、
变量表注入、无未捕获异常。
